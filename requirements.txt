fastapi==0.111.0
python-dotenv==1.0.1
uvicorn[standard]==0.29.0
pydantic==2.7.1
python-multipart==0.0.9
httpx==0.27.0
requests==2.31.0
streamlit==1.35.0

# Optional real tool integrations (graceful fallback if absent)
python-nmap==0.7.1

# Report formats
fpdf2==2.7.9

# Database — SQLAlchemy ORM (SQLite default, PostgreSQL-ready via DATABASE_URL)
sqlalchemy==2.0.30
aiosqlite==0.20.0
alembic==1.13.1

# Auth — password hashing + JWT
# (bcrypt used directly, not via passlib — passlib 1.7.4's bcrypt backend is
# broken against bcrypt 4.x+ and hasn't been meaningfully maintained since 2020)
bcrypt==5.0.0
python-jose[cryptography]==3.3.0
